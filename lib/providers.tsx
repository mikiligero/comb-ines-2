"use client";

// Placeholder — sin Supabase ni TanStack Query.
// El estado de sesión lo gestiona NextAuth en el servidor.
// Zustand sigue aquí para el workout engine (estado cliente puro).
export function Providers({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
